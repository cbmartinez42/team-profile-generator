const inquirer = require('inquirer');
const fs = require('fs');
const generateHtml = require('./utils/generateHTML');
const jest = require('jest');

const employees = [];




function createTeam() {
    addManager();
     
};


function renderHtml() {

};